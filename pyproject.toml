[project]
name = "vintest"
version = "0.0.0"
requires-python = ">=3.14"

[dependency-groups]
dev = [
    "mdformat>=1.0.0",
    "mdformat-frontmatter>=2.0.10",
    "mdformat-mkdocs>=5.1.4",
    "pre-commit>=4.6.0",
]
docs = [
    "zensical>=0.0.43",
]
